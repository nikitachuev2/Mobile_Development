name: vision_nav
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Иконки
  cupertino_icons: ^1.0.8

  # --- БАЗА ДАННЫХ (SQLite) ---
  sqflite: ^2.3.0
  path: ^1.9.0

  # Хеширование паролей (SHA-256)
  crypto: ^3.0.3

  # Плеер для воспроизведения шагов и звука стен
  audioplayers: ^6.0.0

  # Вибрация при столкновении
  vibration: ^3.1.4

  # 🔥 CSOUND УБРАН ИЗ-ЗА НЕИСПРАВИМОЙ ОШИБКИ
  # csounddart: ^0.0.14

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Подключаем звуковые файлы
  assets:
    - assets/sounds/steps.mp3
    - assets/sounds/wall.mp3
    - assets/sounds/colon.mp3

  # Если понадобятся шрифты
  # fonts:
  #   - family: Example
  #     fonts:
  #       - asset: fonts/Example.ttf
